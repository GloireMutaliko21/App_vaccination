import 'package:flutter/material.dart';
import 'package:v_connect/Screens/Signup/components/body.dart';

class SenregistrerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Body(
          child: Column(),
        ),
      ),
    );
  }
}
